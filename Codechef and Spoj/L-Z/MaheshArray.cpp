#include<iostream>
#include<algorithm>
#include<math.h>
#define ll long long int

using namespace std;

int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		int n;
		cin>>n;
		ll ar[n];
		ll a[pow(2,n)];
		for(int i=0; i<pow(2,n), i++)
		{
			cin>>a[i];
		}
		sort(a,a+pow(2,n));
		
		
	}
}

